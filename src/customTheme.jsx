import { createTheme } from '@mui/material/styles';

// Create a custom theme.
const customTheme = createTheme({
  typography: {
    fontFamily: 'Inter, sans-serif', // Specify Inter as the default font family.
  },
});

export default customTheme;
